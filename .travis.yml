language: python

python:
- "3.5"
- "3.6"
- "3.7"
- "3.8"

install:
  - pip install -r requirements.txt

# Disabled because travis-ci cannot accept dynamic stdin (in order to get Yahoo verifier code for OAuth)
#script:
#  - pytest -s

script: "true"
